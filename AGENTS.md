# GAKKI

本仓库发布通用 Codex 前端实现插件。技能负责设计和工程判断，Node CLI/MCP 负责可重复的图像处理与实际运行观察。应用页面、业务模型、设计系统和输入素材属于目标项目。

- 当前实现仅在 `plugins/gakki/`。先根据用户目标选择最小有效改动，不为未出现的需求增加抽象、服务或流程。
- 公开源码、测试、示例和文档不得包含真实业务项目名称、代码、路径、图片、截图或运行记录。测试使用合成图像和通用页面夹具。
- 本地历史、研究、安装日志和分发包放 `achieve/`，保持 Git 忽略；它们不是实现依赖。仅在用户明确需要历史时读取。
- 保留既有未提交工作；替换的历史内容先归档，不覆盖失败记录。目标项目的输出放独立运行目录。
- 观测到的几何与推断的布局分开，功能、资源、响应式、视觉判断和用户接受分别报告。比较指标与源码指纹不能代替真实运行或证明构建来源。
- 素材保持真实 Alpha 和来源像素。动态文本与数据不得烘焙进静态装饰；复杂图像处理按实际用途选工具。
- 修改后运行 `npm run check`，其中包含公开文件边界检查、图像测试、真实浏览器和 stdio 发行入口测试。技能 frontmatter 保持有效；影响页面时在目标项目验证实际渲染。
- `npm run debug -- <operation> input.json` 直接执行源码；`npm run build` 后可用 `npm run debug:mcp` 检查协议。`npm run dev:update` 更新本地安装，新的 Codex 任务加载更新后的技能和工具；不声称已有任务自动热更新。
- 分发使用显式文件白名单，保留全部打包依赖许可。不要自动提交、推送或公开发布。
